The function returns undefined because JavaScript uses automatic semicolon insertion.
The 2*n + 1 is on the line after the return so JavaScript puts a semicolon on the line with the return.
This makes the line return undefined before the 2*n + 1 can be run.
